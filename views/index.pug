html
   head
        include partials/head.pug
        include partials/modals.pug
        body   
            div(class="jumbotron text-center") 
                h1(class="display-4") Welcome to Simple Drinks API
                p(class="lead") This is an api for simple one alcohol cocktails.
                hr(class="my-4")
                p It uses utility classes for typography and spacing to space content out within the larger container.
                    div(class="container")
                        table(class="table")
                            thead(class="thead-dark")
                                tr
                                    th(scope="col") METHOD
                                    th(scope="col") ROUTE
                                    th(scope="col") RETURN
                                    th(scope="col") INFO  
                            tbody
                                tr
                                    th(class="badge badge-pill badge-primary")
                                        a(scope="col") GET  
                                    th 
                                         a(scope="col" href="/drinks") /drinks
                                    th(scope="col") Find all Drinks
                                    th(scope="col" type="button" class="badge badge-pill badge-info" data-toggle="modal" data-target="#allDrinksModal") Click for info    
                                tr
                                    th(scope="col" class="badge badge-pill badge-primary") GET
                                    th(scope="col") /drinks/alcohol
                                    th(scope="col") Find drink by alcohol
                                    th(scope="col" type="button" class="badge badge-pill badge-info" data-toggle="modal" data-target="#drinksByAlcohol") Click for info
                                tr
                                    th(scope="col" class="badge badge-pill badge-warning") PUT
                                    th(scope="col") /drinks
                                    th(scope="col") Change drink information by name
                                    th(scope="col" type="button" class="badge badge-pill badge-info" data-toggle="modal" data-target="#putDrinks") Click for info
                                tr
                                    th(scope="col" class="badge badge-pill badge-success") POST
                                    th(scope="col") /drinks
                                    th(scope="col") Find drink by name
                                    th(scope="col" type="button" class="badge badge-pill badge-info" data-toggle="modal" data-target="#postDrinks") Click for info
                                tr
                                    th(scope="col" class="badge badge-pill badge-danger") DELETE
                                    th(scope="col") /drinks
                                    th(scope="col") Delete by name
                                    th(scope="col" type="button" class="badge badge-pill badge-info" data-toggle="modal" data-target="#deleteDrinks") Click for info
                    hr         